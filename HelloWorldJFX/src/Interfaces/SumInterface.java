package Interfaces;

/**
 * A functional interface called ReportInterface that returns an int value.
 */
public interface SumInterface {
    //abstract method returning an int value
    int calculateSum(int i, int n);
}
